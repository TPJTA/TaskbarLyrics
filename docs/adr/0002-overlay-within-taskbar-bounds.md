---
status: accepted
---

# Overlay within the taskbar bounds

The lyric window will be an independent transparent window visually overlaid within the current Windows taskbar bounds, rather than a child window embedded into Explorer. This preserves the appearance of a taskbar component while avoiding direct ownership by Explorer, at the cost of tracking taskbar geometry and visibility changes.
